brew tap homebrew/dupes

brew install telemachus/homebrew-desc/brew-desc

# meld
brew install python --universal --with-brewed-openssl --with-brewed-tk
brew install pygtk --universal
brew install meld hicolor-icon-theme

# wine
#brew install wine --devel
#brew install winetricks

# Other apps
brew install mysql node macvim

# Shells
brew install zsh zsh-completions
brew install bash
brew install homebrew/versions/bash-completion2

# Completions
brew tap homebrew/completions
brew install rails-completion rake-completion bundler-completion gem-completion pip-completion ruby-completion vagrant-completion tmuxinator-completion

# git
brew install git git-flow-avh

# Tools
brew install wget coreutils md5sha1sum ack rename tree cheat awk the_platinum_searcher
brew install findutils homebrew/dupes/grep --with-default-names
brew install task

# Command line enhancement
brew install liquidprompt autojump stormssh osquery
brew install tmux clipper
launchctl load ~/Library/LaunchAgents/homebrew.mxcl.clipper.plist

########
# apps #
########

# Graphviz - tool for graphs
brew install graphviz

# Tool for backing up mac
brew install mackup

# Download subtitles for movies
brew install submarine subliminal

############
# Finalize #
############

# link to apps folder
brew linkapps
